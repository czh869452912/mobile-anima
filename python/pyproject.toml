[project]
name = "anima-host"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "numpy>=2.1",
  "pillow>=10.4",
  "pytest>=8.3",
  "torch>=2.6",
  "transformers>=4.52",
  "onnx>=1.17",
  "onnxruntime>=1.22",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
